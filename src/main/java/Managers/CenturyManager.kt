package Managers

import Units.Century

/**
 * Construct for maintaining references to all Century units in the game.
 * Intended to make finding individual Centuries easier and faster.
 */
object CenturyManager {
    private val CenturyList = ArrayList<Century>()
}